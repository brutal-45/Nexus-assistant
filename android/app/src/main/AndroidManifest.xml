<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Allow app to check if any other app can handle URLs -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Lets the in-app updater hand the downloaded APK to the package
         installer. Runtime-gated per app on Android 8+ ("Install unknown
         apps"); declared only, nothing is requested until an update. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Needed only for devices running Android 9 (API 28) or lower -->
    <!-- Currently used for storing chat sessions (/Download) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="true"
      android:dataExtractionRules="@xml/backup_rules_12_plus"
      android:fullBackupContent="@xml/backup_rules_legacy"
      android:networkSecurityConfig="@xml/network_security_config"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode|density"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Host-scoped deep link for the Hugging Face "Use this model" flow.
             Scoped to host=hub so this is not a bare-scheme handler. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="nexus" android:host="hub" />
        </intent-filter>
        <!-- Checkout callback for the in-app PalsHub purchase flow. Scoped to
             host=checkout so it is routed to AuthSessionModule, not the
             hub deep-link path. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="nexus" android:host="checkout" />
        </intent-filter>
      </activity>

        <!-- Exposes the downloaded update APK (app-private updates/ dir) to
             the system package installer for the in-app update flow. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Optional native library for OpenCL (GPU acceleration) -->
        <uses-native-library
            android:name="libOpenCL.so"
            android:required="false" />

        <!-- Optional native library for Hexagon DSP (NPU acceleration) -->
        <uses-native-library
            android:name="libcdsprpc.so"
            android:required="false" />
    </application>

</manifest>
